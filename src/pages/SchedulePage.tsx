import ScheduleComponent from "../components/schedule/ScheduleComponent.tsx";

const SchedulePage = () => {
    return (
        <div className={'container'}>
            <ScheduleComponent/>
        </div>
    );
};

export default SchedulePage;